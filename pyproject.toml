[project]
name = "registrypol"
version = "1.0.1"
description = "Windows Registry Policy parser and emitter for Python"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.3.5",
    "ruff>=0.11.2",
    "ty>=0.0.1a21",
]
